# frontend/nginx.conf
server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # This tells Nginx: If a file isn't found, fallback to index.html (React Router takes over)
        try_files $uri $uri/ /index.html;
    }
}